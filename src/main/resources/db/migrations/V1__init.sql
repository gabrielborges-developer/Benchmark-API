create table tb_benchmarks (
    id bigint generated by default as identity,
    fps_avg integer not null,
    resolution tinyint check (resolution between 0 and 3),
    graphics_preset varchar(255),
    software_name varchar(255),
    setup_id bigint,
    primary key (id)
);

create table tb_setup (
    id bigint generated by default as identity,
    price float(53) not null,
    memory varchar(255),
    mother_board varchar(255),
    processor varchar(255),
    video_card varchar(255),
    primary key (id)
);

alter table if exists tb_benchmarks
   add constraint FKimc85fgg5g9qhw4v8xox3lbqu
   foreign key (setup_id)
   references tb_setup;